/*Forms use the autofocus attribute for HTML5This script allows browsers that don't support autofocus yet to fall back gracefully.Rails currently uses the autofocus="autofocus" syntax when generating tags with the form helpers.*/function hasAutofocus() { 	var element = document.createElement('input'); 	return 'autofocus' in element;}$(function(){ 	if(!hasAutofocus()){		$('input[autofocus="autofocus"]' ).focus();	}});